<Window x:Class="RsyncShell.App.Dialogs.RenameRemoteItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource RenameRemoteTitle}"
        Width="470"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource NewName}" Margin="0,0,0,7" />
        <TextBox x:Name="NameInput" Grid.Row="1" Padding="7,4" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="{DynamicResource Cancel}" Width="88" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="{DynamicResource Rename}" Width="88" IsDefault="True" Click="RenameButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
