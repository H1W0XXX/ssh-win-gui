<Window x:Class="RsyncShell.App.Dialogs.KeywordHighlightingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource KeywordHighlightingDialogTitle}"
        Width="880"
        Height="560"
        MinWidth="700"
        MinHeight="430"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource KeywordHighlightingDescription}"
                   TextWrapping="Wrap"
                   Foreground="#4C5967" />
        <TextBlock Grid.Row="1"
                   Text="{DynamicResource OneKeywordPerLine}"
                   Margin="0,5,0,12"
                   Foreground="#7A8490" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource GreenKeywords}" FontWeight="SemiBold" Foreground="#0E9B62" Margin="2,0,0,6" />
            <TextBlock Grid.Column="2" Text="{DynamicResource RedKeywords}" FontWeight="SemiBold" Foreground="#D44848" Margin="2,0,0,6" />
            <TextBlock Grid.Column="4" Text="{DynamicResource YellowKeywords}" FontWeight="SemiBold" Foreground="#A57A00" Margin="2,0,0,6" />

            <TextBox x:Name="GreenKeywordsInput" Grid.Row="1"
                     AcceptsReturn="True" AcceptsTab="False"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Cascadia Mono" Padding="7" />
            <TextBox x:Name="RedKeywordsInput" Grid.Row="1" Grid.Column="2"
                     AcceptsReturn="True" AcceptsTab="False"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Cascadia Mono" Padding="7" />
            <TextBox x:Name="YellowKeywordsInput" Grid.Row="1" Grid.Column="4"
                     AcceptsReturn="True" AcceptsTab="False"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Cascadia Mono" Padding="7" />
        </Grid>

        <TextBlock x:Name="ValidationText" Grid.Row="3"
                   Foreground="Firebrick" TextWrapping="Wrap" Margin="0,9,0,0" />

        <Grid Grid.Row="4" Margin="0,14,0,0">
            <Button Content="{DynamicResource RestoreDefaults}"
                    HorizontalAlignment="Left" Padding="14,5"
                    Click="RestoreDefaultsButton_OnClick" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource Cancel}" Width="92" Margin="0,0,8,0" IsCancel="True" />
                <Button Content="{DynamicResource Save}" Width="92" IsDefault="True" Click="SaveButton_OnClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
